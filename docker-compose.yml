version: '3.8'

services:
  SentiScore:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # 持久化模型缓存，避免重复下载
      - ./cemotion_cache:/app/.cemotion_cache
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=false
      # 多源下载配置 - 使用国内外镜像
      - MODEL_DOWNLOAD_STRATEGY=cn_priority
      - HF_ENDPOINT=https://hf-mirror.com
      - HF_MIRROR=https://hf-mirror.com
    restart: unless-stopped